[install]
registry = { url = "https://registry.npmjs.org/", token = "$BUN_AUTH_TOKEN" }
linker = "hoisted"

# TEMPORARY — #2267 canary, diag branch only.
[test]
preload = ["./packages/rpx/test/helpers/canary.preload.ts"]
